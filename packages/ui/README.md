# @generate-web-ai/ui

Reusable, accessible UI primitives — the design-system layer that every
client site's `packages/blocks` are built from.

Empty scaffold for now: no components have been implemented yet.
