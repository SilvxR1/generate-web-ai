# @generate-web-ai/config

Shared configuration for the monorepo.

Currently provides:

- `tsconfig.base.json` — the strict TypeScript base config extended by every
  other package and app.

Planned, not added yet: a shared Tailwind preset (design tokens) and a shared
lint/format config, once those are introduced.
