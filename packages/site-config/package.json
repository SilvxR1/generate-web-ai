{
  "name": "@generate-web-ai/site-config",
  "private": true,
  "version": "0.0.1",
  "description": "Typed SiteConfig schema — the data shape a client site is described by (brand, theme, pages, blocks, features), consumed by @generate-web-ai/renderer to produce Astro components.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
