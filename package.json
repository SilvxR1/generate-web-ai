{
  "name": "generate-web-ai",
  "private": true,
  "version": "0.0.1",
  "description": "Reusable AI-assisted website production system for small and medium-sized businesses (monorepo).",
  "packageManager": "pnpm@11.22.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "check": "turbo run check"
  },
  "devDependencies": {
    "turbo": "2.10.11"
  }
}
